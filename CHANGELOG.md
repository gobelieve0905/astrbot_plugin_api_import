# 更新日志

## Unreleased

### 2026-09-15

- 新增受控通道的只读工具搜索、接口定义查询和参数/等值约束预检；不发业务请求、不授予凭证，正式调用仍重新校验权限与代理。
- 移除 Meta 工具说明和分页提示中的跨账户、素材排行等任务流程，保留平台接口语义和安全规则。
### 2026-09-14

- 任务网关额度改为管理员配置 task_max_calls（默认 200，最高 10000），明确返回额度超限、耗尽、过期与权限撤销错误码；保留每次操作权限和固定 Meta 代理检查。

- 收紧响应敏感字段匹配，保留 keywords 等普通业务字段及上游 file 数据，仅去除结果顶层宿主路径；补充非对象请求与断开连接处理。

- 新增独立代码执行客户端使用的 v1 Unix 任务网关，限定操作、固定参数、有效期和调用额度；修改/停用操作使既有任务授权失效。
- 网关返回完整单页数据并移除宿主路径、敏感字段与配置凭据；继续使用原权限检查及固定 Meta 代理，不自动重试或保存网关结果文件。

## Unreleased

### 2026-09-14（0.1.3 修复）

- 为原 Meta 账户列表和 Insights 工具增加 result_page 分段读取已保存完整响应的能力；结果绑定当前工具定义和 node_id，读取前后重查后台及关联权限，不接受任意文件路径，不发起网络请求，窗口有界且明确远端分页范围。

- Meta 重复返回字段按原顺序去重，仍拒绝超限、非法字段和字段展开；补充直接使用 Insights 广告级报表与已确认 campaign 筛选规则的路径说明，要求保留跨账户查询覆盖清单。

- 补充 Insights 跨账户素材聚合与排序说明，区分单账户排序、最终 Top N 和 Meta #100 排序参数错误；固定代理只读验证中筛选与消耗排序返回 200，未改变权限。

- 修复 Meta sort、filtering、breakdowns 等数组参数递归校验时 key 未赋值异常；保留嵌套鉴权覆盖与删除状态拦截。
- Meta 返回独立分页覆盖信息，区分远端下一页与本地预览截断；截断时提示缩小 limit 重取当前页，防止跳过未读数据。账户发现预览上限至少 64,000 字符，工具说明要求核实跨账户项目关联，不能以账户名称关键词排除账户。

- 修复 Meta 请求忽略宿主 HTTP 代理导致连接超时：新增后台 Meta 固定代理节点选择，所有 Meta 账户共用管理员选定节点；未选择时停止请求，节点失败不切换、不直连回退、不重试，其他接口保留原网络路径；补充不含凭据的网络错误类型和路径提示。
- 为模型补充 SDK 适用对象，明确用户账户发现使用 /me/adaccounts；拦截错误的 /me/ad_accounts 并提示选择已授权正确操作，不自动切换或开启权限。

- Meta 权限审计：拒绝无法逐项授权的 8 个批量写入接口，复制要求关联创建权限，递归检查删除状态；后台更新时取消旧工具的本地等待任务，明确远端已执行请求不能撤回。
- 增加全部 714 条路线的停用、跨接入与固定路由回归、关联权限排队撤销及等待传输取消验证，附完整权限范围与剩余边界报告。

- Meta 操作详情增加官方接入、Token 与字段参考目录网址，并补充方法影响、共享对象授权范围和参数类型说明，列出全部适用 SDK 对象及方法对应的固定版本参考网址，沿用手动复制方式；说明展示不改变实际调用权限。

- 精简文档排版：同一操作内合并重复网址，移除复制按钮与重复提示，保留点击或键盘聚焦后全选网址、手动复制；账户详情、账户设置和快速接入统一显示。

- 文档入口：账户详情、账户设置与平台快速接入统一展示可选中的只读网址及复制按钮，复制受限时提示手动复制；AppLovin 标注为官方字段与接口说明，Meta SDK 保留 SDK 标注，不修改宿主沙箱权限。

- 修复标准 AppLovin 广告报表因模型传入 `filter_package_name` 别名或单个筛选字符串而在本地校验失败的问题：转换为 `filter_campaign_package_name` 及单元素数组，保留原筛选值。
- 新旧参数同时提供、含逗号的歧义单值、未知参数及 Token 覆盖仍拒绝；不放宽 Meta、手动接口或停用工具的校验。
- 未定义参数错误列出允许的顶层参数，帮助模型修正调用；新增对应失败请求结构及权限边界回归测试。

## 0.1.3 — 2026-09-14

- 按用户指定将当前插件版本更新为 0.1.3；平台卡片同排等高、按钮底部对齐。
- 增加筛选结果全选/取消全选及半选状态，保留筛选外权限；接入、账户设置和详情增加业务分类。
- Meta 目录补充 11 类插件业务分类、中文用途说明及官方 SDK 参考，分类仅用于导航，不改变运行时权限。

### 2026-09-14（Meta 接入）

- 平台快速接入增加 Meta 广告管理：固定官方 Business SDK v26.0.1 / Graph v26.0 操作目录，1490 个 SDK 请求别名合并为 714 个请求方法与路径权限项，全部默认关闭。
- 沿用独立 Token、后台中文名称、英文调用名称和多账户管理；大量操作支持搜索、方法筛选和批量启停筛选结果，保留隐藏项开关。
- Meta 路由及参数结构由后端固定；禁止外部 Token、方法覆盖、通用 Graph batch、字段展开等绕过路径，关联查询和删除状态额外检查权限。
- 工具在发送前再次检查有效性与当前后台权限，拦截旧引用及已停用的排队请求；不影响已发出的请求，不自动重试写入。
- 支持 Meta 查询/表单 JSON 参数、受限 base64 文件上传、游标分页、业务错误识别和响应凭据脱敏；不依赖飞书卡片插件。
- 扩大配置容量以支持完整 Meta 目录，附固定来源、许可证和可复现目录生成脚本；高级请求封装仍按权限边界拒绝，不提供无限制透传。

### 2026-09-13

- API 0.1.2：移除首页调用权限按钮及全局权限弹窗，保留账户设置和单项操作启停。
- API 0.1.2：精简账户详情页重复标题，突出账户摘要和操作筛选；平台加载错误仅在平台接入区域显示，支持原位重试。

- 账户操作页增加名称、调用名称、地址及用途搜索，支持状态和请求方法组合筛选、结果计数与一键清除。
- 调用名称直接展示在操作标题下，调整列表信息层级和窄屏布局，方便定位工具。

### 2026-09-12

- 精简平台卡片重复标题与留白，统一账户卡片和紧凑操作列表样式，适配深色与窄屏。
- 保存等成功提示改为浮动提示，4 秒后自动消失；新提示重置计时，错误提示保留。

- 平台快速接入新增独立调用名称，后台自定义名称支持中文且不传给 Agent；调用名称仅英文字母和下划线，自动追加操作标识。
- 平台选择和账户概览改为卡片网格，提供平台搜索、添加账户、调用名称预览及账户级前缀修改。
- 保留旧账户调用名、Token 和权限；仅修改后台名称不会改变已设置的调用名称，同名调用冲突拒绝保存。

### 2026-09-11（0.1.2 候选修订）

- 修正广告报表筛选：移除标准 filter_application，明确推广应用包名与广告系列筛选；兼容已有标准预设，保留自定义映射、账户与权限。
- HTTP 错误返回有长度限制、凭据脱敏的上游详情，改善参数校验说明；不重试、不保存失败响应文件。

- 实际注册调用名称按自定义账户名和操作名生成，支持逐工具自定义英文调用名称；中文显示名称保留，同名账户自动区分，修改后旧调用引用失效。

- 移除插件专用结果状态协议，改用 AstrBot 标准 CallToolResult/isError；不依赖展示插件，保留独立试调用。

- 将操作列表移入独立账户详情页，账户列表仅展示概况与进入入口。
- 工具提供“账户自定义名称 / 操作自定义名称”用于调用状态展示，配置内部标识不变。
- 使用标准工具结果表达 API 错误状态，显示方式交由客户端处理。
- 合并原 0.1.1 与 0.1.2 更新说明，统一记录更改日期。

## 0.1.2 — 2026-09-11

- API 页面按接入账户分组，折叠展示操作，简化长说明和内部标识的默认展示，适配窄屏。
- 同一平台支持多个独立命名账户，各账户分别管理 Token 和操作权限；更换 Token 可一次更新账户下全部标准操作。
- 支持自定义账户名称及 API 显示名称，模型说明同步包含账户名称，内部工具标识保持稳定。
- 兼容 0.1.1 标准 AppLovin 预设的原接入编号，不清空接口、Token 或权限；查看页面不写配置。
- 新增账户整体删除、按账户搜索及权限分组，复用版本冲突检测与原子保存。

- 移除自动接入、OpenAPI/Swagger 文档发现、普通文档模型解析及模型选择入口。
- 新增默认「平台快速接入」，首个平台 AppLovin Report 只需填写 Report Key / Token。
- 内置广告投放、发布商、MAX 收益、Cohort 收益、展示、会话与留存六个只读操作；逐项开关，默认全部关闭，保存后立即生效。
- 预设请求地址、Token 位置、常用报表字段和分页参数；日期与筛选在调用时提供。支持重复接入多个账号。
- 保留已有接口、可视化表单、cURL、JSON、方法筛选及调用权限管理，配置保存仍有冲突检测和失败恢复。
- 保留数组连接与可选请求体能力，移除只为文档解析使用的 PyYAML 依赖。

## 0.0.1 — 2026-09-11

- 新增 AstrBot 插件页面「API 管理」，支持搜索、新增、编辑、启停和删除接口。
- 提供可视化表单、cURL 草稿解析、JSON 编辑三种填写方式。
- 页面保存立即更新工具并持久化原有配置；增加版本冲突检测、失败恢复和旧工具引用失效处理。
- 保留原有 JSON 配置入口及已有接口，支持插件重载后继续使用。

### 基础能力

- 新增平台无关的 JSON 接口定义与动态 AstrBot 工具注册。
- 支持路径、Query、Header、JSON、表单映射与 JSON Schema 参数验证。
- 支持异步 HTTP 执行、结果提取、受限预览与完整单次响应保存。
- 新增管理员工具列表、实际试调用和执行摘要命令。
- 重载/卸载清理自有工具，配置错误与同名冲突阻止注册。

版本按用户要求从初始开发占位 0.1.0 调整为 0.0.1；正式验收和标签由用户管理。
