# 更新日志

## Unreleased

当前开发目标为用户指定的 0.0.1，尚未创建发布标签。

- 自动接入支持普通 API 文档网页、粘贴说明/参数表/请求示例，通过 AstrBot 默认模型生成待核对草稿，不要求用户提供 OpenAPI 文件。
- 文档网页输入移至主表单；展示参数说明、默认值、路径原文依据及解析限制。模型不接收专用输入框中的 Key，不获得工具或聊天历史。
- 普通文档复用公共参数以减少模型输出，允许较慢模型完成解析，并区分模型超时与请求失败提示。
- 保留结构化文档直接解析；限制网页大小和正文长度，读取或模型解析失败不保存、不执行业务接口。

- 新增默认「自动接入」：Target URL + API Key，发现同源 OpenAPI/Swagger 文档并生成操作、参数、请求体和 Key 映射，支持补充文档 URL 或 JSON/YAML。
- 新增调用权限管理和请求方式筛选，按服务与 GET/POST 等方法批量开关；新识别操作默认关闭，批量保存原子生效。
- 文档发现不携带 Key、不执行业务请求；没有文档时展示 OPTIONS Allow 提示，不猜测接口定义或实际授权范围。
- 支持常见数组分隔序列化、可选请求体缺省省略；保留手动表单、cURL、JSON 和已有接口。

## 0.0.1

- 新增 AstrBot 插件页面「API 管理」，支持搜索、新增、编辑、启停和删除接口。
- 提供可视化表单、cURL 草稿解析、JSON 编辑三种填写方式。
- 页面保存立即更新工具并持久化原有配置；增加版本冲突检测、失败恢复和旧工具引用失效处理。
- 保留原有 JSON 配置入口及已有接口，支持插件重载后继续使用。

### 基础能力

- 新增平台无关的 JSON 接口定义与动态 AstrBot 工具注册。
- 支持路径、Query、Header、JSON、表单映射与 JSON Schema 参数验证。
- 支持异步 HTTP 执行、结果提取、受限预览与完整单次响应保存。
- 新增管理员工具列表、实际试调用和执行摘要命令。
- 重载/卸载清理自有工具，配置错误与同名冲突阻止注册。

版本按用户要求从初始开发占位 0.1.0 调整为 0.0.1；正式验收和标签由用户管理。
