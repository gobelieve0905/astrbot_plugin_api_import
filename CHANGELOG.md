# 更新日志

## Unreleased

### 2026-09-11（0.1.2 候选修订）

- 移除插件专用结果状态协议，改用 AstrBot 标准 CallToolResult/isError；不依赖展示插件，保留独立试调用。

- 将操作列表移入独立账户详情页，账户列表仅展示概况与进入入口。
- 工具提供“账户自定义名称 / 操作自定义名称”用于调用状态展示，内部工具标识不变。
- 使用标准工具结果表达 API 错误状态，显示方式交由客户端处理。
- 合并原 0.1.1 与 0.1.2 更新说明，统一记录更改日期。

## 0.1.2 — 2026-09-11

- API 页面按接入账户分组，折叠展示操作，简化长说明和内部标识的默认展示，适配窄屏。
- 同一平台支持多个独立命名账户，各账户分别管理 Token 和操作权限；更换 Token 可一次更新账户下全部标准操作。
- 支持自定义账户名称及 API 显示名称，模型说明同步包含账户名称，内部工具标识保持稳定。
- 兼容 0.1.1 标准 AppLovin 预设的原接入编号，不清空接口、Token 或权限；查看页面不写配置。
- 新增账户整体删除、按账户搜索及权限分组，复用版本冲突检测与原子保存。

- 移除自动接入、OpenAPI/Swagger 文档发现、普通文档模型解析及模型选择入口。
- 新增默认「平台快速接入」，首个平台 AppLovin Report 只需填写 Report Key / Token。
- 内置广告投放、发布商、MAX 收益、Cohort 收益、展示、会话与留存六个只读操作；逐项开关，默认全部关闭，保存后立即生效。
- 预设请求地址、Token 位置、常用报表字段和分页参数；日期与筛选在调用时提供。支持重复接入多个账号。
- 保留已有接口、可视化表单、cURL、JSON、方法筛选及调用权限管理，配置保存仍有冲突检测和失败恢复。
- 保留数组连接与可选请求体能力，移除只为文档解析使用的 PyYAML 依赖。

## 0.0.1 — 2026-09-11

- 新增 AstrBot 插件页面「API 管理」，支持搜索、新增、编辑、启停和删除接口。
- 提供可视化表单、cURL 草稿解析、JSON 编辑三种填写方式。
- 页面保存立即更新工具并持久化原有配置；增加版本冲突检测、失败恢复和旧工具引用失效处理。
- 保留原有 JSON 配置入口及已有接口，支持插件重载后继续使用。

### 基础能力

- 新增平台无关的 JSON 接口定义与动态 AstrBot 工具注册。
- 支持路径、Query、Header、JSON、表单映射与 JSON Schema 参数验证。
- 支持异步 HTTP 执行、结果提取、受限预览与完整单次响应保存。
- 新增管理员工具列表、实际试调用和执行摘要命令。
- 重载/卸载清理自有工具，配置错误与同名冲突阻止注册。

版本按用户要求从初始开发占位 0.1.0 调整为 0.0.1；正式验收和标签由用户管理。
